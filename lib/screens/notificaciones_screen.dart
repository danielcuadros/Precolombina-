import 'package:flutter/material.dart';

class Notidicaciones extends StatelessWidget {
  const Notidicaciones({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hola"),
    );
  }
}
