import 'package:flutter/material.dart';

class Talento extends StatelessWidget {
  const Talento({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Talento"),
    );
  }
}
